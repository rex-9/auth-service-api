module MessageService
  class AppInstall < Base
    CREATED = "app_install.created"
    UPDATED = "app_install.updated"
    CREATE_FAILED = "app_install.create_failed"
    FETCHED = "app_install.fetched"
  end
end
